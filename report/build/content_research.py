# -*- coding: utf-8 -*-
"""GENERATED FILE — do not edit by hand.

Written by `venv/bin/python -m research.chapters` from
`research/results/results.json`. Every number here was produced by an
experiment in `research/experiments/`; editing this file would break
that guarantee. Change the prose in research/chapters.py and regenerate.
"""

BLOCKS = [
    ('h1', 'RESEARCH METHODOLOGY'),
    ('h2', '1.1 What is being studied'),
    ('p', 'This project builds a closed-loop marketing framework for newly launched products, where almost no behavioural history exists. Four modules act on one audience: segmentation, campaign automation, analytics and decision support, and content generation. Each module was developed separately and evaluated on its own; the contribution of this stage of the work is that they now run as one system over a single audience, and that every claim about them is reproducible from one command.'),
    ('h2', '1.2 The audience, and what it is made of'),
    ('p', 'The study audience is the **Digital Marketing Campaign** dataset — 8,000 customers with recorded website behaviour, email engagement, purchase history and a conversion outcome. Those customers are imported as the customer base of a demonstration e-commerce store, so that the same audience flows through all four modules exactly as a real one would.'),
    ('p', 'This is the single most important methodological point in the project, and it must be stated before any result is quoted. The dataset is real: real people, whose behaviour was really measured. But it was measured as **per-customer totals** — 25 website visits, 5.5 pages per visit, 9 email opens — and not as an event log. Nobody recorded when visit 14 happened or which page it was.'),
    ('table', {'caption': 'Table R1 — What is measured data and what is reconstructed by the importer.', 'header': ['Quantity', 'Status', 'Source'], 'rows': [['Sessions, page views, time on site', 'Measured', 'WebsiteVisits, PagesPerVisit, TimeOnSite'], ['On-site clicks', 'Measured', 'ClickThroughRate × page views'], ['Purchases', 'Measured', 'PreviousPurchases'], ['Email opens and clicks', 'Measured', 'EmailOpens, EmailClicks'], ['Conversion outcome', 'Measured', 'Conversion'], ['Acquisition channel', 'Measured', 'CampaignChannel'], ['Timestamp of each visit', 'Reconstructed', 'Spread over a 90-day window, seeded per customer'], ['Which page each visit landed on', 'Reconstructed', "Cycled through the store's real pages"], ['Scroll depth', 'Reconstructed', 'Derived from dwell time'], ['Order of email opens and clicks', 'Reconstructed', 'Interleaved across the window'], ['Basket value of a purchase', 'Reconstructed', "Drawn from the store's catalogue prices"]]}),
    ('note', '**Consequence for the results.** Anything that depends only on the totals — segment membership, conversion rate by segment, funnel counts — rests on real measurements. Anything that depends on event *order* or *timing* — attribution paths, journey length, inter-event gaps — is a property of the reconstruction as much as of the data, and is reported as such throughout Chapter 3.'),
    ('p', "To keep that distinction in the data rather than only in prose, the importer writes one event per *visit* carrying that visit's totals, instead of one event per page view. A live browser reports one event per page and no count; the feature definition sums the two identically, so a single definition serves both without either pretending to be the other."),
    ('h2', '1.3 Provenance is recorded, never asserted'),
    ('p', 'Imported customers and live browser sessions share one database. Every visitor and every funnel event therefore carries a `source` column with the value `dataset` or `live`, and that value is **derived at write time from the visitor the event belongs to** — it can never be supplied by the caller. A test fails the build if a dataset-derived customer ever produces a row claiming to be live observation.'),
    ('p', 'Every figure in the dashboard carries the resulting label, and the analytics layer refuses to report attribution below five converting journeys rather than returning a number it cannot support.'),
    ('h2', '1.4 Reproducibility'),
    ('p', 'All randomness is seeded (seed 42). The importer is deterministic given the source CSV and idempotent on re-run. Confidence intervals use 10,000 bootstrap resamples, and the policy comparison runs 30 independent seeds. `make research` regenerates every table and every figure in this chapter and the two that follow.'),
    ('h1', 'EXPERIMENTAL SETUP'),
    ('h2', '2.1 The experiments'),
    ('p', 'Eight experiments cover the four modules. Each writes its tables to `research/results/`, and every figure in the next chapter is drawn from those tables rather than plotted by hand, so a chart cannot drift away from the number it shows.'),
    ('table', {'caption': 'Table R2 — The seven experiments.', 'header': ['ID', 'Question', 'Data', 'Primary metric'], 'rows': [['E1', 'Does the hybrid beat its own components?', '8,000-customer dataset', 'Conversion separation, silhouette'], ['E2', 'The two Module 1 defects, reproduced', '8,000-customer dataset', 'Segment survival, accuracy, certainty'], ['E3', 'Which automation policy wins, and at what cost', 'Module 2 simulator, 30 seeds', 'Conversions per 1,000 sends'], ['E4', 'Do the attribution models agree?', 'Module 3 simulator + live journeys', 'MAE vs ground truth, TVD'], ['E5', 'Prediction quality and its transfer', 'Module 3 features + live audience', 'ROC-AUC, PR-AUC, Brier'], ['E6', 'TF-IDF versus Sentence-BERT', '527-row goal/tone corpus', 'Macro-F1, McNemar'], ['E7', 'Engagement leakage and text signal', '12,000-row engagement corpus', 'R², Spearman, Holm–Bonferroni'], ['E8', 'Targeting by uplift versus by predicted response', 'Hillstrom, 64,000 randomised', 'Qini, incremental response']]}),
    ('h2', '2.2 Choice of statistics'),
    ('p', 'Four tools are used, each chosen because it makes the fewest assumptions the data can violate.'),
    ('bullets', ['**Percentile bootstrap** rather than a normal-theory interval. Conversion separation, silhouette and macro-F1 are not means and have no closed-form standard error; resampling makes no distributional claim at all.', '**Paired bootstrap and Wilcoxon signed-rank** for the policy comparison. Each seed puts the same 8,000 customers through all three policies, so the runs are paired; ignoring that wastes power and overstates the p-value.', '**Exact McNemar** for two classifiers on one test set. Only the disagreements carry information, and with roughly a hundred test rows the exact binomial is correct where the asymptotic approximation is not.', "**Cliff's delta** alongside every p-value. With thirty seeds almost any difference reaches significance; the effect size is what says whether it matters."]),
    ('h2', '2.3 Metrics, and why not the obvious ones'),
    ('bullets', ['**Conversion separation**, not cluster quality, for segmentation. Conversion is used only to *evaluate* segments — never as a clustering feature and never as a rule input — so a segmentation that separates it has found something it was not told.', '**Conversions per 1,000 sends**, not conversion rate, for the policy comparison. User-level conversion rises simply by sending more messages, which would hand the win to the policy that sends most rather than the one that sends best.', '**Macro-F1**, not accuracy, for goal and tone. The majority class is 59% of the goal corpus, so accuracy flatters a model that handles rare classes badly — and the rare classes are the ones a marketer would want detected.', '**Spearman**, not R², for engagement. The model exists to rank candidate captions, not to predict an engagement rate in absolute terms, and rank correlation is what ranking needs.']),
    ('h1', 'RESULTS'),
    ('h2', '3.1 Segmentation — the hybrid against its components'),
    ('table', {'caption': 'Table R3 — Each method scored on the same 8,000 customers, with 95% bootstrap intervals.', 'header': ['Method', 'Segments', 'Separation', '95% CI', 'Cold-start segment'], 'rows': [['rules only', '5', '0.3837', '[0.309, 0.464]', 'yes'], ['k-means only', '4', '0.0693', '[0.049, 0.090]', 'no'], ['hierarchical only', '4', '0.0343', '[0.019, 0.059]', 'no'], ['hybrid (vote)', '5', '0.3838', '[0.307, 0.463]', 'yes']]}),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e1_segmentation_ablation.png', 'caption': 'Figure R1 — Conversion separation by method. The hybrid and the rules are indistinguishable; clustering alone separates almost nothing.', 'width': 6.0}),
    ('p', 'The hybrid separates conversion by 0.3838, against 0.3837 for the rules alone. The difference is within noise. Clustering on its own reaches 0.0343.'),
    ('note', '**This is a negative result, and it is reported as the headline rather than buried.** On the separation metric the clustering contributes nothing. The hybrid earns its place in the system on different grounds — it produces a calibrated confidence from the level of agreement between three independent methods, and the vote is what makes cold start explicit — but it does not separate conversion better than a handful of interpretable rules.'),
    ('p', 'Silhouette is 0.0869. The clusters overlap heavily in feature space, and all three methods disagree for 35.3% of customers. The segments are commercially useful without being geometrically clean, and reporting only the separation would hide that.'),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e1_segment_profile.png', 'caption': 'Figure R2 — Segment sizes and conversion rate per segment.', 'width': 6.0}),
    ('h2', '3.2 Two defects, reproduced and measured'),
    ('h3', '3.2.1 A segment deleted by its own vote'),
    ('p', 'The original agreement vote tested whether the two clusterings agreed *before* it tested whether the rules had identified a cold-start customer. Clustering cannot represent “there is not enough evidence about this person” — it must place everyone somewhere — so whenever the two clusterings happened to agree, they overruled the rules. Of 163 cold-start customers the rules detect, that ordering keeps 127; on the live audience the segment went to zero. Novel Contribution 1 was being erased from its own output.'),
    ('p', 'Resolving cold start immediately after unanimity keeps all 163. Those customers convert at 53.4% against 88.4% for everyone else, so the segment the original code discarded is the one with the most distinctive behaviour in the dataset.'),
    ('note', 'The general principle, and the one worth defending in a viva: **a method that cannot represent a finding does not get a vote on it.**'),
    ('h3', '3.2.2 Target leakage in the confidence model'),
    ('p', 'The Random Forest was trained with the rule label among its input features while the target was the hybrid consensus — so it was largely reading off its own answer. Reproducing both versions on the same split shows accuracy barely moves (0.9213 against 0.9200), but certainty changes by a factor of 150: 450 customers emerge at confidence 1.00 with the rule label among the inputs, against 3 without it.'),
    ('p', 'Accuracy is not what the defect corrupts. The confidence attached to every downstream decision is — and a model whose accuracy is honest while its confidence is not will be trusted in exactly the cases where it should not be. That is also why the defect survived review: nothing fails, no test goes red, and the only symptom is a confidence column that looks impressive.'),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e2_defects.png', 'caption': 'Figure R3 — Both defects reproduced against their fixes.', 'width': 6.0}),
    ('h2', '3.3 Automation policies — outcome against cost'),
    ('table', {'caption': 'Table R4 — Policy comparison over 30 paired seeds on the Module 2 simulator.', 'header': ['Policy', 'Conv. per 1,000 sends', '95% range', 'Messages/user', 'Decision rules'], 'rows': [['fixed', '7.15', '[6.75, 7.49]', '4.0', '1'], ['trigger', '10.45', '[9.62, 11.17]', '1.32', '4'], ['hybrid', '7.99', '[7.45, 8.41]', '3.05', '9']]}),
    ('table', {'caption': 'Table R5 — Head-to-head, paired by seed.', 'header': ['Comparison', 'Mean difference', '95% CI', 'Wilcoxon p', "Cliff's δ", 'Extra rules'], 'rows': [['hybrid − fixed', '0.837', '[0.727, 0.944]', '1.86e-09', '0.991', '8'], ['hybrid − trigger', '-2.462', '[-2.574, -2.347]', '1.86e-09', '-1.0', '5'], ['trigger − fixed', '3.299', '[3.146, 3.447]', '1.86e-09', '1.0', '3']]}),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e3_policy_comparison.png', 'caption': 'Figure R4 — Efficiency across seeds, and what each policy costs to run.', 'width': 6.0}),
    ('table', {'caption': 'Table R6 — The same three policies measured on the imported audience in the running system.', 'header': ['Policy', 'Sent', 'Clicks', 'Conversions', 'Conv. per 1,000 sends', 'Rules'], 'rows': [['fixed', '500', '144', '36', '72.0', '1'], ['trigger', '400', '111', '44', '110.0', '4'], ['hybrid', '400', '108', '39', '97.5', '8']]}),
    ('note', "**The simulation and the live build disagree about which policy wins.** Both metrics are volume-controlled, so the difference lies in how response is modelled — the simulator's segment-level propensities against per-customer rates drawn from each customer's own recorded email history. Neither is an observation of real people reacting. The ranking is evidently not robust to that choice, and that instability is the result. Quoting whichever run supports the preferred conclusion would be the one genuinely dishonest option available here."),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e3_sparsity.png', 'caption': 'Figure R5 — Efficiency as the behavioural signal is suppressed. A policy that reacts to behaviour should degrade as behaviour stops being observable.', 'width': 6.0}),
    ('h2', '3.4 Attribution — four models, four answers'),
    ('table', {'caption': 'Table R7 — Attribution models scored against known channel influence on simulated journeys.', 'header': ['Model', 'MAE', '95% CI', 'Channels'], 'rows': [['first_touch', '0.0178', '[0.0112, 0.0268]', '5'], ['linear', '0.0178', '[0.0086, 0.0292]', '5'], ['last_touch', '0.026', '[0.0094, 0.0448]', '5'], ['markov', '0.0525', '[0.0345, 0.0791]', '5']]}),
    ('p', 'On the live journeys of 7,811 converting customers the models disagree over 68% of all attributed credit at the widest pair, and 35% on average. Last-touch hands the great majority of credit to email; first-touch spreads it almost evenly across the acquisition channels.'),
    ('note', 'A company reading only its last-touch dashboard would conclude that email is responsible for nearly everything, and would cut the acquisition spend that built the audience email later converted. The disagreement between models is not a defect in one of them — it is the finding.'),
    ('p', '20% of converting journeys have a single touchpoint. On those, every model agrees by arithmetic, and that agreement must never be read as corroboration.'),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e4_attribution.png', 'caption': 'Figure R6 — The same journeys under four attribution models, and their error where ground truth exists.', 'width': 6.0}),
    ('h2', '3.5 Prediction, and what the move to another audience costs'),
    ('table', {'caption': "Table R8 — Conversion and drop-off classifiers against a baseline, fitted on Module 3's simulator.", 'header': ['Target', 'Model', 'ROC-AUC', '95% CI', 'PR-AUC', 'Brier'], 'rows': [['conversion', 'baseline (stratified)', '0.4983', '[0.4772, 0.5221]', '0.1147', '0.205'], ['conversion', 'logistic regression', '0.9651', '[0.9541, 0.9743]', '0.8371', '0.0409'], ['conversion', 'random forest', '0.9722', '[0.9642, 0.9793]', '0.8667', '0.0375'], ['conversion', 'xgboost', '0.9725', '[0.9654, 0.9793]', '0.8624', '0.038'], ['drop_off', 'baseline (stratified)', '0.4898', '[0.4687, 0.5107]', '0.6328', '0.4715'], ['drop_off', 'logistic regression', '0.9306', '[0.9168, 0.9442]', '0.9377', '0.0829'], ['drop_off', 'random forest', '0.9876', '[0.9829, 0.9915]', '0.9917', '0.0362'], ['drop_off', 'xgboost', '0.99', '[0.9867, 0.9929]', '0.9941', '0.0355']]}),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e5_prediction.png', 'caption': 'Figure R7 — Discrimination against a stratified baseline, with bootstrap intervals.', 'width': 6.0}),
    ('table', {'caption': 'Table R9 — The same models applied to the imported audience — a different distribution from the one they were fitted on.', 'header': ['Prediction', 'Min', 'Median', 'Max', 'Above threshold'], 'rows': [['conversion', '0.0103', '0.9126', '0.9956', '5606 of 8000 (≥ 0.5)'], ['drop-off risk', '0.0006', '0.0441', '0.9911', '911 of 8000 (≥ 0.6)']]}),
    ('p', 'The models discriminate well on the distribution they were fitted on. Applying them to another audience is a transfer across distributions: the ranking remains usable, the absolute probabilities are not calibrated for it, and any threshold set on the training distribution should be treated as arbitrary here. The system raises a calibration warning rather than reporting a confident number when the prediction distribution is degenerate.'),
    ('h2', '3.6 Goal and tone — the simpler model wins, and it matters which metric says so'),
    ('table', {'caption': 'Table R10 — Classifier comparison. Accuracy and macro-F1 tell different stories.', 'header': ['Target', 'Model', 'Accuracy', 'Macro-F1', 'Weighted F1', 'Test rows'], 'rows': [['campaign_goal', 'majority baseline', '0.5877', '0.1481', '0.4351', '114'], ['campaign_goal', 'tfidf + logistic', '0.7719', '0.4998', '0.7628', '114'], ['campaign_goal', 'sbert + xgboost', '0.7719', '0.4042', '0.7391', '114'], ['tone', 'majority baseline', '0.3864', '0.1115', '0.2154', '44'], ['tone', 'tfidf + logistic', '0.8182', '0.7315', '0.8153', '44'], ['tone', 'sbert + xgboost', '0.7727', '0.5912', '0.7482', '44']]}),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e6_goal_tone.png', 'caption': 'Figure R8 — Accuracy against macro-F1, with the majority-class baseline marked.', 'width': 6.0}),
    ('table', {'caption': 'Table R11 — Exact McNemar between the two approaches on the same test rows.', 'header': ['Target', 'TF-IDF right, SBERT wrong', 'SBERT right, TF-IDF wrong', 'p'], 'rows': [['campaign_goal', '8', '8', '1.000'], ['tone', '5', '3', '0.727']]}),
    ('note', 'Neither comparison is significant. The two approaches are statistically indistinguishable on this corpus, so selecting TF-IDF is a decision about cost, speed and interpretability rather than about accuracy — which is a stronger and more defensible claim than asserting the simpler model is more accurate.'),
    ('h2', '3.7 Engagement — a leak, and a dataset with no signal'),
    ('table', {'caption': 'Table R12 — The engagement regressor with and without the outcome columns among its features.', 'header': ['Feature set', 'Features', 'R²', 'Spearman', 'MAE'], 'rows': [['with outcome columns (original)', '17', '0.9901', '0.9977', '0.01847'], ['text features only (corrected)', '13', '-0.1676', '0.012', '0.39307'], ['baseline (predict the mean)', '0', '-0.0', '0.0', '0.32783']]}),
    ('p', 'With the outcome columns present the model reports R² = 0.9901. Removing them gives R² = -0.1676 [-0.4081, -0.0715] and a rank correlation of 0.0120 [-0.0276, 0.0539] — an interval spanning zero, so no ranking skill is demonstrated on held-out data.'),
    ('p', 'The failure was invisible in training and fatal in use. A caption that has not been posted has no like count, so at prediction time those columns were filled with zeros — far outside anything the model had seen — and the score driving 45% of every content ranking became noise. Its weight was reduced to 0.20 once the honest number was known.'),
    ('p', 'Testing each of the 8 text features against the target individually, 0 survive Holm–Bonferroni correction. This is a property of the dataset rather than a modelling failure: no model can extract a signal that is not there, and the correct response is to reduce the weight the score carries rather than to keep tuning.'),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e7_engagement.png', 'caption': 'Figure R9 — The leak, and the absence of text signal.', 'width': 6.0}),
    ('h2', '3.8 Who to target is not who will convert'),
    ('p', 'The recommender ranks customers by predicted conversion and gives the strongest action to the top of that ranking. That is the intuitive thing to do and it answers the wrong question. What matters is not who will convert but for whom the action *changes* whether they convert — a customer certain to buy anyway gains nothing from a discount, and the discount is wasted on them.'),
    ('p', "Answering that needs counterfactuals, and this project's own audience cannot supply any: every imported customer received one treatment, nobody recorded which, and no comparable customer received an alternative. No model can recover a causal effect from data where the cause never varied. The experiment therefore moves to the **Hillstrom MineThatData** dataset — 64,000 customers randomly assigned to one of three arms (mens email, womens email, no email) with observed visits. Random assignment is what makes the counterfactual estimable."),
    ('table', {'caption': 'Table R13 — Targeting policies scored on held-out customers. Qini is incremental responders above random targeting; the final column is what a 30% email budget buys.', 'header': ['Policy', 'Qini', '95% CI', 'Extra visits per 1,000 targeted'], 'rows': [['uplift (S-learner)', '93.62', '[56.11, 129.76]', '103.45'], ['predicted response (current policy)', '75.91', '[38.8, 109.98]', '91.26'], ['uplift (T-learner)', '70.36', '[30.19, 112.29]', '92.0'], ['random targeting', '-9.75', '[-48.65, 26.95]', '57.76']]}),
    ('figure', {'path': '/Users/arkamzakir/Documents/Research/Research/research/figures/fig_e8_uplift.png', 'caption': 'Figure R10 — Qini curves and what each policy buys at a fixed budget.', 'width': 6.0}),
    ('p', 'Ranking by uplift and ranking by predicted response select substantially different people: the two scores correlate at Spearman 0.40, and at a 30% budget the two policies share only 56% of their chosen customers — so about 44% of the list would be emailed by one and not the other.'),
    ('note', '**What this does and does not establish.** The best uplift learner buys more incremental visits than the current policy, but their Qini intervals overlap, so the ordering of the two is not established on this split. The two uplift learners also disagree with each other, one of them falling below the current policy — so “use uplift modelling” is not a conclusion on its own. What the data does support is the weaker and more useful claim: these are different policies that target different people, and the difference is large enough to matter.'),
    ('table', {'caption': 'Table R14 — The three-arm version: which action each customer should receive, rather than whether to act.', 'header': ['Assigned action', 'Customers', 'Share', 'Observed uplift per 1,000'], 'rows': [['Mens E-Mail', '13601', '70.8%', '71.27'], ['Womens E-Mail', '5054', '26.3%', '87.47'], ['No E-Mail', '545', '2.8%', '0.0']]}),
    ('p', 'This is the next-best-action problem proper: not send or do not send, but which of several actions. Note that the policy assigns a share of customers to *no email at all* — an output the current rule cannot produce, because every customer is given some action regardless of whether acting helps.'),
    ('p', "Hillstrom's actions are mens and womens email, not this project's premium, personalised, reactivation and reminder. The experiment demonstrates the method on real randomised data and shows that the current policy is answering a different question from the one it should. It does not produce a policy deployable to the imported audience, and nothing in this report should be read as claiming it does."),
    ('h1', 'DISCUSSION'),
    ('h2', '4.1 What the evidence supports'),
    ('bullets', ['**The closed loop runs.** One audience passes through segmentation, content generation, campaign planning and analytics, and the analytics output feeds back into which platforms get written for next. That is the architecture the report proposes, executing.', '**Cold start is handled explicitly, and it had to be defended.** The engine declines to cluster below thirty customers and says why; individual customers with thin histories are decided by rules and flagged. The vote-order defect showed how easily that contribution can be destroyed by a pipeline that looks correct.', '**Attribution model choice changes the conclusion.** On the same journeys the four models disagree over a third of attributed credit on average. Any single-model dashboard is a decision, not a measurement.', '**Simpler models won twice.** TF-IDF matched Sentence-BERT on goal and tone, and interpretable rules matched the full hybrid on conversion separation. Neither result was expected.', '**The recommender was answering the wrong question.** Ranking customers by predicted conversion and ranking them by the *incremental effect* of the action select materially different people — they share only about half their choices at a realistic budget. This is the clearest direction for future work the project produced.']),
    ('h2', '4.2 What the evidence does not support'),
    ('bullets', ['**That the hybrid segmentation separates conversion better than rules alone.** It does not, on this dataset. Its value lies in calibrated confidence and explicit cold start.', '**That any one automation policy is best.** The simulator and the live build disagree, and the ranking depends on how response is modelled rather than on the policies themselves.', '**That engagement can be predicted from caption text.** Not on this corpus, where no text feature survives correction.', '**That the prediction thresholds transfer.** They were set on the distribution the models were fitted on and do not carry to another audience. The production recommender was changed to rank rather than threshold as a direct consequence.', '**That uplift modelling is straightforwardly better.** The best uplift learner beat the current policy, but the intervals overlap and the second uplift learner did worse. The framing is right; the evidence for any particular learner is not yet strong.']),
    ('h1', 'THREATS TO VALIDITY'),
    ('h2', '5.1 Construct validity'),
    ('p', 'The reconstruction of event timing is the principal threat. Attribution operates on journeys whose *order* this project created, so the attribution results characterise the reconstruction as well as the data. The counts underlying those journeys are real; their sequence is not observed. A dataset with genuine timestamped events would settle what this study can only bound.'),
    ('p', 'Conversion separation depends on segment definitions that a human wrote. The rules were not tuned against the conversion column — which is what keeps the metric from being circular — but they were written by someone who knows the domain, and that is a weaker guarantee than a preregistered rule set.'),
    ('h2', '5.2 Internal validity'),
    ('p', 'Two defects found in this work were both invisible to the metrics being watched at the time, and both made results look better. That is evidence that other such defects may remain. Every correction now carries a regression test, and the leakage guard in the engagement model raises an exception rather than a warning if an outcome column re-enters the feature set.'),
    ('p', 'Campaign response in both the simulator and the live build is modelled, not observed. No claim about open or click rates in this report is a measurement of human behaviour.'),
    ('h2', '5.3 External validity'),
    ('p', 'One dataset, one product category, one language. The 87.6% conversion rate in the source data is far above any plausible e-commerce baseline, which suggests the dataset is either filtered or synthetic in origin; conclusions about absolute rates should not leave it. Comparisons *between* methods on the same data are the results that travel.'),
    ('p', 'The goal corpus has 453 labelled rows and the tone corpus 174, with one tone class represented by a single example. Macro-F1 on a corpus that small carries wide uncertainty, and the per-class numbers for rare classes should be read as indicative.'),
    ('h1', 'LIMITATIONS'),
    ('bullets', ['**Event timing and ordering are reconstructed**, so attribution path statistics are partly a property of the importer.', '**Campaign response is modelled**, in both the simulator and the live build. Nobody in this study opened a real email.', '**Conversion and drop-off models were fitted on a simulator** and transfer poorly in calibration, though the ranking survives.', '**The engagement dataset carries no usable text signal**, so that component of the content score is close to arbitrary and is weighted accordingly.', '**`humorous` tone has a single training example** and cannot be learned or evaluated.', '**Open-rate tracking under-reports** by design: most mail clients block the pixel, so click-through is the reliable engagement signal.', '**Three real platform datasets ship unused** — they are comment-level scrapes that pair no post text with post engagement.', "**No action was ever randomised on this project's own audience**, so the next-best-action recommendation cannot be validated on it at all. E8 borrows a dataset where treatment *was* randomised, and the actions there are not this project's actions."]),
]
